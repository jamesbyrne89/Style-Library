import React, { Component } from 'react';
import './style.css';

const Header = () => {
    return ( <
        <
        header className = "App-header" > < / header >
    );
}

export default Header;