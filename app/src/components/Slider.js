import React from 'react';

const Slider = (props) => {
    const { hex, name, rgb } = props.col
    return (
        <div class="menu-transition" id="menu-transition"></div>
    )
}

export default Slider;